{
    // tsx грузит TypeScript и сам разрешает алиас app/* по paths из tsconfig.json,
    // поэтому отдельный резолвер путей не нужен. Типы он не проверяет — это делает
    // npm run typecheck.
    "require": ["tsx/cjs"],
    "spec": "test/**/*.spec.ts",
    "color": true
}
