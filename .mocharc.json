{
    // tsconfig-paths/register обязателен: сам ts-node алиас app/* не разрешает,
    // и без него mocha падает на загрузке первого же spec-файла.
    "require": ["ts-node/register", "tsconfig-paths/register"],
    "spec": "test/**/*.spec.ts",
    "color": true
}
