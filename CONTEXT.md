# Конвертация шрифтов

Смысл сервиса — переносить файлы шрифтов между форматами, покрывая как можно больше пар форматов, включая вышедшие из обращения. Всё остальное в репозитории (Telegram, пользователи, база) обслуживает приём и выдачу шрифтов и в этот домен не входит.

Глоссарий описывает предметную область, а не устройство кода: имена классов, DI, пайплайны и Telegram — в `docs/architecture.md` и `docs/flows.md`.

## Язык

### Основные понятия

**Конвертация шрифтов**:
Главная и единственная задача сервиса: перенос шрифта из одного формата в другой. Ценность измеряется широтой матрицы пар — тем, из скольких форматов во сколько сервис умеет переносить.
_Избегать_: сохранение форматов, консервация форматов, font-format conservation — читается ещё и как «запись файла на диск», поэтому не годится как название задачи.

**Конвертация**:
Единичный перенос одного шрифта из исходного формата в целевой, порождающий новый файл. Всегда один шаг: промежуточных форматов нет, а исходный и целевой форматы не могут совпадать.
_Избегать_: генерация, экспорт, преобразование.

**Пара конвертации**:
Упорядоченная пара «исходный формат → целевой формат». Единица поддержки в этом домене: сервис поддерживает не формат вообще, а конкретные пары. Сейчас матрица полна для пяти поддерживаемых форматов — 20 пар.
_Избегать_: направление, маршрут.

**Движок конвертации**:
Внешний инструмент, который выполняет сам перенос между форматами. Домен не реализует конвертацию сам: он выбирает пару, проверяет вход и передаёт работу движку, поэтому набор достижимых пар ограничен возможностями движка.
_Избегать_: конвертер (так называется часть домена, а не движок).

### Форматы

**Формат**:
Форма представления шрифта, между которыми сервис его переносит: WOFF, WOFF2, OTF, TTF, EOT, SVG.
_Избегать_: расширение, тип файла, MIME-тип.

**Поддерживаемый формат**:
Формат, участвующий хотя бы в одной паре конвертации. Сейчас это WOFF, WOFF2, OTF, TTF и EOT. Объявить формат в списке недостаточно, чтобы он стал поддерживаемым.
_Избегать_: доступный формат, известный формат.

**Намеченный формат**:
Формат, признанный частью домена, но пока не участвующий ни в одной паре конвертации: заявка на будущую поддержку, а не наследие. Сейчас такой один — SVG.
_Избегать_: неподдерживаемый формат, legacy-формат.

**Расширение**:
Суффикс имени файла и единственный признак, по которому домен определяет формат шрифта. Содержимое файла при этом не читается, поэтому формат здесь — утверждение об имени файла, а не о его байтах.
_Избегать_: формат (это разные вещи: расширение — способ узнать формат, а не сам формат).

### Файлы

**Шрифт**:
Файл в одном из форматов. Для этого домена он непрозрачен: домен опознаёт формат и передаёт файл движку, но не читает, что внутри.
_Избегать_: фонт, font.

**Исходный шрифт**:
Шрифт, поданный на конвертацию. Домен его только читает: не изменяет, не переименовывает и не удаляет.
_Избегать_: оригинал, source.

**Результат конвертации**:
Новый файл в целевом формате, созданный конвертацией. Его имя не связано с именем исходного шрифта. Существующий файл конвертация не перезаписывает никогда; когда результат перестаёт быть нужным, домен не определяет.
_Избегать_: сгенерированный шрифт, выходной файл.

### Вне модели

Термины из типографики, которые в разговоре о шрифтах возникают сами собой, но которых у этого домена нет. Если они понадобятся, это будет расширение модели, а не уточнение формулировки.

**Семейство**:
Не входит в домен: сервис не читает метаданные шрифта и не знает, к какому семейству тот принадлежит.

**Начертание**:
Не входит в домен: сервис не различает жирность, наклон и ширину и не может отобрать шрифт по ним.

**Глифы и метрики**:
Не входят в домен: сервис не открывает содержимое шрифта, поэтому не может ни перечислить глифы, ни сравнить их до и после конвертации. Сохранность глифов и метрик при конвертации домен сейчас никак не выражает и не проверяет.
